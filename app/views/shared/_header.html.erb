<header>
  <h3 class="logo">CodeBytes</h3>
  <% if logged_in? %>
    <%= button_to "Log Out", session_url, method: :delete %>
  <% else %>
    <%= link_to "Log In", new_session_url %>
    <%= link_to "Sign Up", new_user_url %>
  <% end %>
</header>
