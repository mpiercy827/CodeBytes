// var Interpreter = function (options) {
//     this.language = options.language;
//     this.loadLanguage();
// }
//
// Interpreter.prototype.loadLanguage = function (first_argument) {
//   // body...
// };

var intInput = function (callback) {
  callback();
}

var intOutput = function (outputData) {
  if (outputData) {
    
  }
}
